{
  "name": "examinu-api",
  "version": "1.0.0",
  "description": "Backend API for a quiz application",
  "main": "server.js",
  "scripts": {
    "start": "fastify start --port 8080 --address 0.0.0.0 server.js",
    "dev": "fastify start --watch --port 8080 --address 0.0.0.0  --log-level info server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "fastify-cli": "^7.4.1"
  }
}
